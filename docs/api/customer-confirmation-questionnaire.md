# 智能体平台与 MES 系统 API 对接确认问题集

> 发出状态：已发送客户（2026-08-21）
>
> 答复状态：客户已于 2026-08-24 14:50 书面答复问题集，并在 2026-08-25 沟通中补充确认。
> 答复整理为 `docs/reference/弘兆MES接口整体说明-V2.md` 的 M1~M20（已确认结论）与
> K1~K7（范围决策），仍未确认项保留在该文档第五章（共 14 项）。
>
> 使用规则：本文件保留原始问题与答复对照，不覆盖原问题。已确认结论的实施依据是 V2 说明与
> 产品文档，不是本文件。

## 一、身份、权限与组织

| ID | 待确认问题 | 主要影响 |
|---|---|---|
| CQ-01 | 是否已有 OAuth 2.0/OIDC 等统一身份认证？员工是直接使用客户账号登录智能体平台，还是需要单独注册？ | Story 2、4、9；可信身份入口 |
| CQ-02 | 统一登录后，智能体平台是否可以保存最小用户映射，用于会话、订阅和定时推送？ | Story 4、8；会话与订阅数据模型 |
| CQ-03 | MES 能否提供稳定的用户唯一标识，以及用户基本信息、账号状态、角色、所属组织和管辖范围？ | Story 1、2、9；身份与 `DataScope` |
| CQ-04 | 用户权限由独立权限接口提供，还是由每个 MES 业务接口根据当前用户自动鉴权和过滤？MES API 的统一认证方式是什么？ | Story 2、3、9；权限责任与 Adapter 鉴权 |
| CQ-05 | 请明确员工、管理者、老板及其他角色分别能够查看哪些数据。一人是否可能拥有多个角色，或同时管理多个车间、多个小组？ | Story 2、7、9；权限矩阵与范围 |
| CQ-06 | MES 是否可以提供完整组织树，并明确工厂、车间、小组、员工之间的关系，以及调岗、借调和历史组织归属的处理方式？ | Story 1、2、9；组织层级与历史范围 |

## 二、MES 业务接口

| ID | 待确认问题 | 主要影响 |
|---|---|---|
| CQ-07 | 当前 MES 接口是否已经覆盖产量、订单进度、工资、排名、全厂总览和导出等需求？ | Story 1、3、5~9；接口覆盖与报表边界 |
| CQ-08 | 业务接口是否支持按时间、员工、组织、订单、款号、工序和状态过滤，并支持一次查询多个对象？ | Story 1、3、9；条件下推和批量查询 |
| CQ-09 | 是否支持分组、汇总、排序、排名等查询？大量列表如何分页，大数据量结果如何导出？ | Story 3、6、8、9；本地加工、分页与导出 |
| CQ-10 | MES 是否可以提供员工、订单、款号、组织、工序等对象的搜索能力，以处理名称、编号和同名候选？ | Story 3、4、7、9；实体解析与追问 |
| CQ-11 | MES 是否有稳定的员工、组织、订单、款号、计划、工单和工序 ID？ | Story 1、3、9；跨 API 关联硬要求 |
| CQ-12 | 请明确订单、款号、计划、工单、工序、工价、员工、产量、工资和进度之间的对应关系。 | Story 1、3、6、7、9；领域关系和指标口径 |
| CQ-13 | 如果一名员工同时参与多个订单，MES 返回的数据能否区分其在不同订单中的产量和工资？ | Story 1、7、9；事实粒度与去重 |
| CQ-14 | MES 是否可以提供业务状态、组织类型、工序、单位等基础字典，以及工资、产量、进度和排名的统一业务口径？ | Story 1、3、6、7、9；枚举与指标版本 |
| CQ-15 | 对于没有专用接口、但能够通过多个 MES 只读 API 得到答案的问题，是否允许智能体平台自主组合、关联和分析？ | Story 3、6、9；Text-to-API 编排授权 |
| CQ-16 | 当无数据、参数错误或用户无权限时，MES 是否能够返回明确结果，便于智能体追问或调整查询？ | Story 1、3~6、9；错误语义与降级 |

## 三、定时任务与事件推送

| ID | 待确认问题 | 主要影响 |
|---|---|---|
| CQ-17 | 定时推送只面向已经登录并订阅的用户，还是需要覆盖客户全部员工？智能体平台是否可以保存用户映射和订阅信息？ | Story 8 条件项；通知对象与数据存储 |
| CQ-18 | 如果需要向从未登录过智能体平台的员工推送，MES 是否可以提供员工同步或其他用户获取方式？ | Story 8 条件项、Story 10；用户同步边界 |
| CQ-19 | 定时任务运行时用户不在线，智能体平台应以什么身份访问 MES，并如何按照目标用户的最新权限查询数据？ | Story 2、8 条件项、Story 10；离线授权 |
| CQ-20 | 工资发布后，MES 是否可以通知智能体平台？采用事件推送还是由智能体平台定时查询？是否还需要生产日报、订单延期等其他通知？ | Story 8 条件项、Story 10；事件与轮询 |
| CQ-21 | 最终需要支持哪些推送渠道？客户是否已有统一消息发送能力？工资通知是否只提示登录查看，不直接展示金额？ | Story 8 条件项、Story 10；渠道与敏感展示 |

## 四、智能体问答边界

| ID | 待确认问题 | 主要影响 |
|---|---|---|
| CQ-22 | 智能体是否需要支持与 MES 业务无关的日常闲聊？如果不支持，希望如何提示或引导用户？ | Story 4；意图分类与非业务提示 |
| CQ-23 | 对于 MES 数据不足、确实无法回答的问题，希望提示缺少数据、要求补充条件、推荐相近查询，还是转人工处理？ | Story 4、6、8；追问和失败体验 |
| CQ-24 | 是否存在明确禁止回答的内容、敏感数据或其他业务边界？ | Story 2、4、9；安全分类与回答策略 |

## 五、请 MES 提供的资料

| ID | 资料 | 当前状态 |
|---|---|---|
| MAT-01 | 现有接口清单及支持状态 | ✅ 已提供（27 个接口，V2 §2） |
| MAT-02 | 统一身份认证及权限说明 | ✅ 已提供（M1/M3/M15/M19） |
| MAT-03 | 用户、角色和组织关系说明 | ⚠️ 部分（组织与用户已明确；角色枚举待确认，第五章 A.1） |
| MAT-04 | 订单、款号、工单、工序和工价等业务关系说明 | ✅ 已提供（M18，V2 §1.5） |
| MAT-05 | 产量、进度、工资和排名的业务口径 | ⚠️ 部分（M6/M7/M9/M18 已确认；次品、达成率、量产状态、在册人数待确认） |
| MAT-06 | 接口文档及基本调用示例 | ✅ 已提供（`docs/reference/AI问答对外接口.md`） |
| MAT-07 | 定时任务、事件通知和消息渠道说明 | ✅ 已答复：MES 无推送能力，本期不做（M17/K1/K6） |
| MAT-08 | 测试环境、测试账号和联调计划 | ❌ 未提供（根地址已确认，app_key 与账号待交付，第五章 B.2 🔴） |

## 答复记录

| ID | 客户答复要点 | 证据/来源 | 确认日期 | 处理结果 |
|---|---|---|---|---|
| CQ-01 | 员工在**客户自己的小程序**内登录，智能体以悬浮窗集成；跳转时携带加密 app_key，前端调 `/api/system/token` 取凭证；无需单独注册 | M10/M15 | 2026-08-25 | 身份入口在 Story 5 重做为客户凭证包 |
| CQ-02 | 可以保存最小用户映射（`uid` ↔ `uname`/company），用于会话、订阅与推送 | M20 | 2026-08-24 | Story 8 用户映射按此实现 |
| CQ-03 | 稳定标识分两层：账号层 PIUSER.ID/CODE（`UserInfoQuery`）、业务员工层 `sys_employee.uid`（`EmployeeQuery`）；角色枚举待补 | M20 / 第五章 A.1 | 2026-08-24 | `employee_id` 绑定 token 的 `user`；角色仅展示 |
| CQ-04 | **由每个业务接口按调用者身份自动鉴权和过滤**；认证为 Bearer + 公共参数 | M1/M3 | 2026-08-24 | 删除我方管辖闭包计算；`DataScope` 改为最小可证明范围 + `mes_filtered` |
| CQ-05 | 我方不关心用户角色；数据范围由 company / dept / `move_admin_role` 共同决定，`"00"` 限查本人 | M11/M19 | 2026-08-24 | 角色不进入 `authorize()` 判定 |
| CQ-06 | **组织只有一层部门（车间 `dept`），没有小组**；`DeptQuery` 含 `pid`；不需要历史组织关系 | M5 / K2 | 2026-08-25 | 全局删除小组粒度与任职有效期 |
| CQ-07 | 27 个只读查询接口已覆盖产量/进度/工资/排名；报表由我方自有机制承担，MES 不提供报表服务 | V2 §2 / K5 | 2026-08-25 | Story 6 保留自建导出链路 |
| CQ-08 | 支持时间区间 + 主键筛选；批量 ID 支持有限，`WorktypeProgressQuery` 需逐物料查询 | V2 §2 | 2026-08-24 | 批量进度靠预算与分批控制（Story 7 风险项） |
| CQ-09 | 列表接口均返回 `total`，`page`/`size` 分页；部分接口带 `footer` 合计；`GongziMxQuery` 支持 `scheme=hz` 汇总，`GongziJeOrderQuery` 已按金额倒序 | M13 / V2 §2.5 | 2026-08-25 | 分页器改为 `total` 累计判定 |
| CQ-10 | 未直接答复搜索能力；`name_pk` 简拼字段存在，检索口径待确认 | 第五章 C.6 | - | 本期只支持精确编号 + 追问 |
| CQ-11 | 有稳定 ID：`uid`/`dept`/`huohao`/`worktype`/`dh`/`jhdh`/`dddh`/制单明细 `id`/`cid` | M18 | 2026-08-24 | 关联键写入字段字典 |
| CQ-12 | 生产计划 → 生产制单 → 制单明细；明细挂货号/工序/床号；员工经 `uid` 关联产量与工资 | M18 | 2026-08-24 | 领域关系与 Mock 数据模型按此重建 |
| CQ-13 | 客户问题集未作答 | 第五章 C.13 | - | 按制单/货号维度区分，联调验证 |
| CQ-14 | 字典接口齐备（`ScTypeQuery`/`RfidWorktypeQuery`/`HuohaoQuery`/`DeptQuery` 等）；工资 = `sl × price`；进度按扫码判定；排名按返回顺序编号 | M6/M7/M9/M18 | 2026-08-25 | 口径写入指标注册表并标记 confirmed |
| CQ-15 | **允许**智能体平台自主组合、关联、分析多个只读 API | M16 | 2026-08-25 | ADR-0005 的临时性说明更新为客户确认 |
| CQ-16 | 成功判断两层（HTTP 200/404 + `code` 0/1 + `message`）；**权限不足无专门错误码**，表现为过滤后无数据 | M12/M14 | 2026-08-25 | Adapter 建立 `message` 映射；M12 范围收窄判定 |
| CQ-17~CQ-21 | MES **无推送能力**：无订阅机制、无事件推送、内部调度器未启用。推送需我方定时轮询 + 对接客户已有消息渠道，但**当前版本不做**（无固定服务账号可离线取 token） | M17 / K1 / K6 | 2026-08-25 | FR-018 标记为当前版本不做；后续版本前置条件为客户提供固定服务账号 |
| CQ-22~CQ-24 | 未答复 | - | - | 维持「仅业务能力 + 结构化拒绝」的产品策略 |

## 仍未确认项

共 14 项，完整列表见 `docs/reference/弘兆MES接口整体说明-V2.md` 第五章；对实现的影响面见
[`customer-api-requirements.md`](customer-api-requirements.md) 的「仍未确认的接口与口径」。

处理客户后续答复时：

1. 权限、租户、敏感字段、保留期和离线授权结论先由人工批准，再更新 `SECURITY.md` 或产品文档。
2. 业务口径更新到 `docs/product/`、`docs/api/field-dictionary.md` 与版本化指标注册表，
   不只写在本答复表中。
3. 接口事实更新到 [`customer-api-requirements.md`](customer-api-requirements.md)、契约与 Adapter。
4. 对既有 Mock 假设有影响时，更新 fixture 与 golden，并保留变更原因。
