# 需求追踪

> 更新日期：2026-08-25。本表用于快速找到实现某类需求的顺序 Story，不参与自动评估。

| 需求 | 实施 Story |
|---|---|
| 客户 27 接口契约、Mock MES 全量、Story 1~4 返工 | `#5` 客户契约对齐与 Mock MES 全量实现 |
| FR-002、FR-003 | `#6` 首条纵切（个人工资汇总与明细） |
| FR-001、FR-005~FR-012 | `#7` 其余 L1 查询能力 |
| FR-004 | 已取消（M7：员工端接口只返回本人数据），管理端排名并入 FR-008 |
| FR-013 | `#6` 可信结果与 Excel，并由 `#7` 接入各能力 |
| FR-014~015 | `#4` 会话、意图、slot、追问与上下文补丁；`#5` 按单层车间与新 slot 修订 |
| FR-016 | `#2` 拒绝语义，`#5` M12 范围收窄判定，`#4` 会话提示 |
| FR-017 | `#8` 快捷问题、历史与收藏 |
| FR-018 | 当前版本不做（K1/K6/M17）；前置条件为客户提供固定服务账号 |
| 使用计量与运营 API | `#1` usage-admin/事件契约骨架、`#4` outbox 事件生产、`#8` ingest/汇总/运营 API/报表 |
| NFR 权限/隔离/脱敏 | `#2` 基线，`#5` 按 MES 行级过滤重定义，`#6` 安全导出 |
| NFR 有界执行/降级/性能 | `#3` 预算，`#4` 模型降级，`#8` 缓存与基线测量（K3 不作门槛），`#9` 上线 |
| 工程骨架与执行内核 | `#1` 工程与 Mock、`#3` 执行内核、`#6` 首条纵切 |
| 模型网关与 report-agent 复用 | `#4` 模型网关与复用、`#9` 生产准入 |
| MES 契约与 Adapter 边界 | `#1` 骨架、`#3` Adapter 边界、`#5` 客户形态契约、`#9` 真实环境接入 |
| 受限 L2 | L1 稳定且产品确认后在 `#8` 增补，非默认完成项 |
| 客户环境联调与生产上线 | `#9` |
