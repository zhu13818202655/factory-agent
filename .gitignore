.env
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.pytest-tmp/
.ruff_cache/
.coverage
htmlcov/
build/
dist/
# macOS Finder metadata (created in every directory the Finder touches, including
# submodules; unanchored so it matches at any depth).
.DS_Store
# Anchored: only the repository-root runtime output directory, never fixtures
# such as tests/data/.
/data/

# agent memory
.workbuddy/
tools/